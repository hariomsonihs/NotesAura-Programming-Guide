// Debug rules - temporarily allow all authenticated users
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

// Use this ONLY for testing, then switch back to secure rules